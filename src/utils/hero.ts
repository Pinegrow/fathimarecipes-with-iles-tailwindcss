import { pg_background_urls } from '../../vuedesigner/pg-tailwindcss/themes/tokens.mjs'

const heroImageUrl =
  pg_background_urls['design-image-large'] || pg_background_urls['design-image']

export { heroImageUrl }
